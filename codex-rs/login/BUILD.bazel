load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "login",
    compile_data = [
        "src/assets/error.html",
        "src/assets/success.html",
        "src/assets/success_legacy.html",
    ],
    crate_name = "codex_login",
    test_shard_counts = {
        # Gateway credential tests exercise real encryption and run serially on Windows.
        "login-unit-tests": 4,
    },
    unit_test_timeout = "long",
)
